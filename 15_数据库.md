# 模块十五：数据库编程

## 学习目标

- 了解 LuaSQL 的安装与基本用法
- 掌握 SQL CRUD 操作
- 理解事务处理（BEGIN/COMMIT/ROLLBACK）
- 了解 SQL 注入防御
- 了解 NoSQL（Redis）支持

---

## 1. LuaSQL 简介

### 概念讲解

LuaSQL 是 Lua 的数据库抽象层，支持 SQLite3、MySQL、PostgreSQL。

安装：
```bash
luarocks install luasql-sqlite3
luarocks install luasql-mysql
```

### 代码示例（概念）

```lua
local luasql = require("luasql.sqlite3")
local env = luasql.sqlite3()
local conn = env:open("test.db")

-- 执行 SQL
conn:execute("CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT)")
conn:execute("INSERT INTO users VALUES (1, 'Alice')")

-- 查询
local cursor = conn:execute("SELECT * FROM users")
local row = cursor:fetch({}, "a")
while row do
    print(row.id, row.name)
    row = cursor:fetch(row, "a")
end

cursor:close()
conn:close()
env:close()
```

---

## 2. CRUD 操作

### 代码示例

```lua
-- INSERT
conn:execute("INSERT INTO users (name, age) VALUES ('Alice', 30)")

-- SELECT
local cursor = conn:execute("SELECT name, age FROM users WHERE age > 20")
local row = cursor:fetch({}, "a")
while row do
    print(row.name, row.age)
    row = cursor:fetch(row, "a")
end
cursor:close()

-- UPDATE
conn:execute("UPDATE users SET age = 31 WHERE name = 'Alice'")

-- DELETE
conn:execute("DELETE FROM users WHERE name = 'Bob'")
```

---

## 3. 事务处理

### 代码示例

```lua
conn:execute("BEGIN")
local ok, err = pcall(function()
    conn:execute("INSERT INTO accounts (id, balance) VALUES (1, 100)")
    conn:execute("UPDATE accounts SET balance = balance - 50 WHERE id = 1")
    -- 检查余额等逻辑
end)
if ok then
    conn:execute("COMMIT")
else
    print("事务失败:", err)
    conn:execute("ROLLBACK")
end
```

---

## 4. SQL 注入防御

### 代码示例

```lua
-- 转义函数
local function quote_string(s)
    if s == nil then return "NULL" end
    return "'" .. tostring(s):gsub("'", "''") .. "'"
end

local user_input = "'; DROP TABLE users; --"
local safe = quote_string(user_input)
print("safe:", safe)  -- "'''; DROP TABLE users; --'"

-- 白名单验证
local function validate_sort_column(col)
    local allowed = {name = true, age = true, id = true}
    return allowed[col] and col or "id"
end
```

---

## 5. Redis 支持

### 代码示例（概念）

```lua
local redis = require("redis")
local client = redis.connect("127.0.0.1", 6379)

-- 字符串
client:set("key", "value")
local val = client:get("key")

-- 哈希
client:hset("user:1", "name", "Alice")
client:hset("user:1", "age", "30")

-- 列表
client:lpush("queue", "task1")
local task = client:rpop("queue")
```

---

## 本章小结

| 功能 | Lua 实现 |
|------|---------|
| SQLite | luasql-sqlite3 |
| MySQL | luasql-mysql |
| PostgreSQL | luasql-postgres |
| Redis | redis-lua |
| 连接池 | 自定义实现 |
| ORM | 自定义或库 |

---

## 课后练习

1. **用** luasql 创建 users 表，插入 3 条数据，用 SELECT 查询并打印结果

2. **实现**一个事务包装函数 `with_transaction(conn, fn)`，自动处理 BEGIN/COMMIT/ROLLBACK

3. **实现**一个 Redis 连接池，支持 acquire 和 release，并限制最大连接数为 10

4. **实现**一个 SQL 转义函数 `quote_value`，根据类型正确转义字符串、数字和 nil

5. **用** luasql 实现一个简单的 ORM 模型类：`Model:new(table_name)`，支持 find(id) 和 all()
